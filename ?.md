``` bash
#include <stdio.h>

int main() {
    int a = 10;
    int b = 20;
    int max = (a > b) ? a : b; // command to check the bigger integer

    printf("The maximum of %d and %d is %d\n", a, b, max);
    return 0;
}
```
