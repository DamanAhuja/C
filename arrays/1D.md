``` 
#include <stdio.h>

int main() {
    int arr3[6]= {1,2,3,4,5,6}; // defining a 3d array
    for (int i = 0;i<6;i++) {
        printf("Number at position (%d) is: %d\n",i,arr3[i]); // printing the elements of array in order
    }

    return 0;
}
```
